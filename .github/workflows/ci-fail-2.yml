# Intentionally failing workflow for failure-agent testing
name: CI Fail 2
on:
  push:
    branches: [ 'head' ]
jobs:
  fail:
    name: fail
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Intentional fail with message
        run: |
          echo "CI Fail 2: intentional failure"
          false
