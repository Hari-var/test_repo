# Intentionally failing workflow for failure-agent testing
name: CI Fail 3
on:
  push:
    branches: [ 'head' ]
jobs:
  fail:
    name: fail
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Intentional failure via script
        run: |
          echo "CI Fail 3: will exit non-zero"
          /bin/false
